name: ci

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  gates:
    name: gates
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: "22.19.0"
          cache: pnpm

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      - name: pnpm test
        run: pnpm test

      - name: node scripts/check-version.mjs
        run: node scripts/check-version.mjs

      - name: node scripts/check-upstream-drift.mjs
        run: node scripts/check-upstream-drift.mjs

      - name: git diff --check
        run: |
          git fetch --no-tags origin main
          git diff --check origin/main...HEAD
          git diff --check
